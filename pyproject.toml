[project]
name = "pdfcontent"
version = "0.1.0"
description = "Auto fix PDF TOC"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fitz>=0.0.1.dev2",
    "pymupdf",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# --- 新增的部分 ---
[tool.hatch.build.targets.wheel]
packages = ["."]
